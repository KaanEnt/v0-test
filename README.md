I want to have a display cards section that lead the nav to different pages. Some examples for these include `pop up hacker house 2026` , `testimonials` , `sponsorships`, `contact us` 

The cards would have either a picture and a title or a title and some text create these examples and two mocks oen being only title and text the other being title and photo. @.specstory/references/display_cards 

Testimonials include
 `AI tinkerers meetups` / `400 plus attendee hackathons` / `Demo day` / `Clay Club` / `Cursor Meetups` / `Supabase Launch Party` / `ElevenLabs Hackathon`

For contact us, we would have the email address, calendly link and linkedin which would direct to my profile, and send a connection request when they click it.

We need to have a proper hero section that Says `showcase your intensity on` and next line in the middle we would have the gooey text change from `product, design, engineering, hustle, organizing, connecting` @.specstory/references/goey_text 

The background should be the flow field for the initial landing page area, have subtler colours with white background and blue particles instead please @.specstory/references/flow_field 

I need the flow field to come back at the bottom when we have scrolled, before that I need framer like effects that make it so we go through the page by scrolling down but it would switch cards as the entire layout instead. ad the bottom one would have half of the page as normal page and the bottom half as the flow field. and the footer section.

The cardstack should be used (for now use mocks) to showcase the prev'ous events that we have done with group and stylistic photos. this should be on a section of the page called `Our previous events` just make it easy for me tho change the copy @.specstory/references/card_stack 

I want to have the design testimonial template to showcase different comments we have had from the past and the shoutout from big companies that have sponsored us in the past. make it so we can add more sponsors in the sliding part on the bottom add clay and cursor in along with vercel tavily factory google microsoft zapier supabase @.specstory/references/design-testimonial 

I need a location tag. this will keep track of time and if it is, justso people would know if we`d be open to hang out or cowork together or not, I want it to become blue on after hours (after 7 is after hours). When it is green it should signal to people that they can come hang out and let us know. this will bring them a popup that they can put their name in and it will send us an automatic message when they do, limit by IP. After hours we are still open to hang out but they have to bring snacks @.specstory/references/location_tag 

I want the hover deck to be at the bottom of the page. for the above footer part that I was talking about. I want multiple of these with varying sizes but keep the overall standard deviation low. I need to have a text on the left middle side. it would be a hero text, gooey text that we used on the original hero, it would read 3 different messages. make it so the messages loop properly and have a strict container that contains them. and to the right of that e would have the hover decks @.specstory/references/hover_deck 

I would also need a testimonial slider that slides between the pictures of the team members. this can be a whole section of the full page, it works better that way. @.specstory/references/testimonial_slider 

The maps would showcase the specific location of the hackerhouse and we need to load data about the stuff around it so people can have a more definite idea on where it is. use the maps component for this. make mock data,  @.specstory/references/map 

let's try to compe up with an overall schema on where everything will go in a rigorous plan and so on

have proper sections so that nothing goes into each other and everything is balanced

I have already said all the references but you had no reference files inside the plan, update that 100%